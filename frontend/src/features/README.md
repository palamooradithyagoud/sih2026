# Feature Modules

This directory is reserved for domain-specific feature modules to be developed in future phases:
- `auth/`: Login, registration, session management, RBAC guards
- `investigation/`: Case files, evidence logs, officer verification workflows
- `graph/`: Interactive Neo4j knowledge graph visualization & querying
- `ai/`: RAG chat interface, crime pattern prediction, suspect timeline analysis
